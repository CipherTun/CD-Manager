# Python parity backend

Required CI/desktop parity engine. It is not embedded in the Android APK because Python is not an Android AAR runtime. GitHub Actions must install Python and run its smoke test.
