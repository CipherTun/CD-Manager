# Node.js parity backend

Required CI/desktop parity engine. It is not embedded in the Android APK because Node.js is not an Android AAR runtime. GitHub Actions must install Node and run its smoke test.
